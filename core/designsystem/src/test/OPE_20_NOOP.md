# OPE-20 operational-loop probe

This file exists to drive a tiny diff through `core/designsystem` so the on-demand
`unit-gate` workflow queues against the `light` runner. The PR will be closed
without merge once the chain (label → slot → connected → green → ephemeral exit)
is captured.

See: /OPE/issues/OPE-20
